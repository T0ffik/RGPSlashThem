import {StyleSheet} from 'react-native';
import {colors} from 'Static/consts/colors';
export const styles = StyleSheet.create({
  title: {fontSize: 40, fontWeight: '400', color: colors.main, marginBottom: 28},
});
